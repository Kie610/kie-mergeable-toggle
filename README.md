# kieMergeableToggle

VRChat アバター用の NDMF プラグイン。

> **alpha** — PC と Quest の実機で、隠しかたが意図どおり効くことを実測しています。

## 何をするもの

手書きアニメーターレイヤーで `GameObject` のオンオフをしているメッシュトグルを、
ビルド時に**別の隠しかたへ機械的に変換**します。

AAO (Avatar Optimizer) の自動メッシュ統合は「アクティブ状態のアニメーション」で
メッシュをグルーピングするため、トグルが違うメッシュ同士は統合されません。
本ツールで変換するとメッシュ側は常時アクティブになり、表示切替は別の機構が担うので、
**トグル挙動を保ったまま AAO がメッシュを統合できる**ようになります。

ポリゴン数を減らすのは削減ツール、マテリアル数を減らすのは統合ツールの仕事で、
どちらも非破壊です。
表示切替条件が一致しない SkinnedMeshRenderer だけは他のどのツールでも統合できません。
そこがこのツールの担当範囲です。

## 使い方

1. アバタールートに `Kie/kieMergeableToggle` コンポーネントを追加
2. 検出されたトグル一覧から変換したいものを選ぶ (クリーンな候補は初期状態でオン)
3. そのままビルドするだけ

一覧には、トグルごとの頂点数と変換対象の合計が出ます。**基本は既定のまま全部変換で構いません。**
外したほうがよいのは「ほとんど着ないのに極端に大きいトグル」だけです
(判断の目安は下の「隠している間のコスト」)。

既存のレイヤー・パラメータ・メニューは変更しません。共有 PhysBone の停止を有効にした
場合だけ、FX の末尾へ専用レイヤーとローカルパラメータを追加します。非破壊 (ビルド時のみ)。

## 隠しかた — infinimation

全頂点のデルタを遠方 (`1e6`) へ向けたブレンドシェイプを生成し、`0⇔100` で切り替えます。
頂点が遠クリップ面のはるか外へ飛ぶのでプリミティブが破棄され、**完全に消えます**。
デルタは有限値です (`+Infinity` は VRChat クライアントの自分視点でレンダラーごと
描画されなくなるため。0.8.1-alpha で変更)。

| ボーン | コンストレイント | マテリアル | ポリゴン | 消えかた | シェーダ依存 |
|---|---|---|---|---|---|
| ±0 | ±0 | 統合される | ±0 | 完全に消える | 無し |

- パフォーマンスランクの計上項目はひとつも増えない (増えるのは VRAM のみ、下記)
- 初期非表示はブレンドシェイプのウェイトとしてそのままシリアライズされるので、
  ワールド入室直後の一瞬だけ見える、といったことが起きません
- 代償は VRAM (1 トグルあたり頂点数×デルタ分)。こちらはランクの計上対象外です

実測 (AAO 併用・PC):

| アバター | SMR | マテリアルスロット | ボーン |
|---|---|---|---|
| Shinano | 11 → **2** | 14 → **6** | 272 → **272** |
| MUMUS_all | 21 → **4** | 36 → **18** | 453 → **453** |

> 0.5.0-alpha より前は、シェイプ(関節)・シェイプ(軸)・UVタイル破棄・NaNimation の
> 4 機構から選ぶ形でした。infinimation がすべての面で上回ったため 1 本化しています。
> Quest (モバイル GPU) でも 2026-08-25 に実機で確認しています。

## 隠している間のコスト

統合したメッシュは**常に表示されたまま**で、トグルは頂点を遠くへ飛ばして見えなくします。
つまり変換したトグルは、**隠している間もその頂点ぶんのコストを払い続けます**。
変換しなければ、隠したメッシュはレンダラーごと消えるのでコストはゼロです。

そのかわり、表示しているときはレンダラーとマテリアルスロットが減ります。
**どちらが得かは、トグルの数ではなく「隠している頂点数」で決まります。**

インスペクタの一覧に、トグルごとの頂点数と変換対象の合計を出しています (0.8.2-alpha)。
判断に迷ったら、この合計のうち普段どれくらいを隠しているかを見てください。

**目安: 合計のうち普段隠している割合が 9 割を超えるあたりで損得が釣り合います。**
つまり**ほとんど着ないトグルが 1 つだけ極端に大きい、という場合を除けば、全部変換して得**です。
外すなら、一覧で頂点数の大きいものから外してください。

<details>
<summary>この目安の実測条件</summary>

Shinano_TEST を 10 体並べ、240 フレーム × 8 反復。A (変換なし) と B (変換あり + AAO) を
同じ反復の中で隣接させて測り、反復ごとの対の差で判定しています
(Unity 2022.3.22f1 / VRCSDK 3.10.4 / AAO 1.9.17)。

**分岐点は「どの資源で見るか」と「どんな構図で測るか」で変わります。単一の数字にはできません。**
塗り面積とフレームあたりの描画回数を振った 4 構成で測った結果:

| 指標 | 分岐点 (隠せる頂点に対する割合) |
|---|---|
| CPU メインスレッド | **89.1〜89.8%** (4 構成すべて。構図に鈍感) |
| wall frame time | 塗り面積 1% で 25.9% / 18% で 58.0% / 描画 1 回では判定不能 |

上の「9 割」は、構図に鈍感だった CPU メインスレッドの値です。
VRChat で混雑時に効いてくるのも主にこの資源です。

これはエディタでの計測であり、実機 (VRChat クライアント) では取り直していません。
またこの機体・この構図での値です。

</details>

## 非表示中は配下コンポーネントも止める

`disableComponentsWhenHidden` (既定オン) は、トグル対象を常時アクティブ化しても
パーティクルやコンタクト等が動き続けないよう、配下コンポーネントの `m_Enabled` を
表示と同じタイミングで切り替えます。ただし、次のコンポーネントは無効化せず、
理由とパスをビルドログへ警告として出します。

- 編集時点で `m_Enabled` が false
- 同じ GameObject に同じ型が複数ある
- `m_Enabled` が既にアニメーションされている

編集時点で既に無効なコンポーネントは、トグル対象を常時アクティブ化しても動き出さないため、
インスペクタでは「要注意」の理由にしません。`m_Enabled` を持たないコンポーネントは
無効化する手段がないため、従来どおり「要注意」になります。

## 非表示中は専用 PhysBone も止める (0.6.0-alpha)

衣装の揺れボーン (PhysBone) はアーマチュア側に付いているため、衣装を隠しても
揺れ続けて CPU を使います。これは元のトグル (`m_IsActive` を 0 にする) でも同じです。

`disablePhysBonesWhenHidden` (既定オン) は、**その衣装のためだけに存在する** PhysBone を
見つけて、隠すのと同じタイミングで `m_Enabled` を落とします。判定は保守側です:

- PhysBone のチェーンを使うレンダラー (ウェイトを持つメッシュ + ボーンにぶら下がる
  オブジェクト) が、**全部そのトグルで隠れる**場合だけ止めます
- 素体など、変換対象トグルの外にも消費者があるボーン (胸・尻尾など) は対象外
- 編集時に無効な PhysBone、`m_Enabled` が既にアニメーションされている PhysBone にも
  触れない設計です

`disableSharedPhysBonesWhenHidden` (既定オン) は、複数の変換対象トグルで共有される
PhysBone も、**所有するトグルが全部非表示になったときだけ**止めます。
`disablePhysBonesWhenHidden` がオフなら、この従属オプションも効果を持ちません。

共有 PB がある場合は、所有トグルの組ごとに FX の末尾へ `MT_PBStop <n>` レイヤーを
1 本生成します。各トグルの非表示状態は `MT_Hidden/<トグルのパス>` float パラメータへ
追従させ、全パラメータが 0.5 より大きい間だけ停止ステートへ入ります。これらは
Animator 内だけのローカルパラメータで、Expression Parameters の枠や同期帯域は消費しません。
元のトグルクリップが FX 以外のコントローラにもある共有グループは、誤停止を避けるため
生成せず、ビルドログへ警告します。生成予定の `MT_Hidden/<トグルのパス>` と同名の
パラメータが FX に既にある場合も、衝突する owner を含む共有グループ全体を生成せず、
同様に警告します。

実測 (保守判定で止められた本数): Shinano 10/61 本、MUMUS_all 68/89 本。

再表示した衣装の揺れはレスト位置から始まります (Unity の仕様で、無効化中の
揺れ状態は保持されないため)。削れるのは PhysBone のシミュレーションに掛かる
実行時 CPU で、パフォーマンスランクは動きません (ランクは無効なコンポーネントも
数える仕様)。

## 注意点

- **インスペクタの一覧は参考表示です。ビルド結果とは一致しません。** 正はビルドログです。
  - **ビルド時にトグルを生成するツールのぶんは一覧に出ません**が、変換はされます。
    編集時にはまだクリップが存在しないためです。実測では Avatar Menu Creator
    (Narazaka) が該当し、衣装アバターで一覧 8 件に対しビルド 18 件になりました。
    MA Object Toggle も同様です。**`m_IsActive` を生成しうる NDMF ツールは無数に
    あるため、一覧を完全にすることは原理的にできません。**
  - **逆に、一覧に出ても変換されないものがあります。** ビルド中に階層を張り替える
    パッケージ (AvatarPoseSystem など) では、編集時とビルド時でパスが別物になるためです。
- 「要注意」と表示される候補は、非表示中も止められないコンポーネントがトグル配下に
  あります。挙動を確認のうえ手動でオンにしてください。
- 「適用不可」と表示される候補に含まれるのは、隠蔽シェイプを追加できないメッシュです
  (対象は頂点を持つ SkinnedMeshRenderer のみ)。放置した候補はビルド時に変換対象から外れます。
- 一覧の「頂点数」は、そのトグルで隠れる頂点の数です。変換すると隠している間も
  このぶんを払い続けます (上の「隠している間のコスト」)。
- PhysBone の停止対象と、共有 PB の `MT_PBStop` レイヤー・所有トグル・PB パスは
  ビルドログに出ます。変換対象外の素体などにも消費者がある PB は止めません。

## 依存

- VRChat SDK - Avatars 3.7 以上
- NDMF (`nadena.dev.ndmf`) 1.14 以上

効果を出すには **Avatar Optimizer** が必要です (依存には入れていません。本ツールは
「AAO が統合できる状態にする」だけで、統合するのは AAO です)。
シェーダには依存しません。

**Modular Avatar** は必須ではありませんが、入っている場合は MA Merge Animator が
持ち込むトグルも扱います。本ツールは MA より後に走るので、ビルド時にトグルを生成する
ツール (MA Object Toggle、Avatar Menu Creator など) のぶんも変換対象になります。

## ライセンス

MIT
