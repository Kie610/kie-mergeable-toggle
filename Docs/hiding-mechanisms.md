# 隠蔽機構 — 仕様と根拠

最終更新: 2026-08-11

## 解きたい問題

SMR を含むゲームオブジェクトの `m_IsActive` をアニメーションでオンオフしている構造を、
別の機構へ置き換える。目的は SkinnedMeshRenderer 数の削減。

AAO の自動メッシュ統合 (`Editor/Processors/TraceAndOptimize/AutoMergeSkinnedMesh.cs`) は
メッシュを activeness でグルーピングしてから統合するため、トグルが異なるメッシュ同士は
統合されない。統合後の SMR 数 ≒ 異なるトグルの数 + 常時オン群。トグルを別機構へ移せば、
そのメッシュは常時オン群に合流できる。

ポリゴン数は削減ツールで、マテリアル数は統合ツールで、いずれも非破壊で解決できる。
**表示切替条件が一致しない SMR だけは他のどのツールでも統合できない。** そこがこの
ツールの存在理由なので、SMR を減らすために他のランク項目を悪化させては本末転倒になる。

要件の優先順位:

1. トグル挙動が保たれること
2. パフォーマンスランクの計上項目に悪影響を与えないこと
3. Mobile (Android / Quest) でも適用できること

## 共通層がやること

### 実行順: Modular Avatar より後

`InPhase(BuildPhase.Transforming).AfterPlugin("nadena.dev.modular-avatar")`。

MA Merge Animator が合流させたコントローラも、MA がリアクティブに生成したトグルも、
合流後の仮想アニメーター上では手書きトグルと区別が付かないので、そのまま候補になる。

0.1.0-alpha までは「手書きレイヤーの状態のまま処理したい」として MA より**前**に
走らせていたが、それだと Merge Animator 経由のトグルが一切見えず、そういう構成の
アバターで何も変換されなかった。実測 (Shinano Variant、FX をどこへ置くか):

| 構成 | 検出件数 |
|---|---|
| ディスクリプタの FX レイヤー | 9 |
| Merge Animator (Absolute) | 0 → **9** |
| Merge Animator (Relative、ルート直下) | 0 → **9** |
| Merge Animator (Relative、子オブジェクト、relativePathRoot=ルート) | 0 → **9** |

変換本体は `AnimationIndex` 経由で候補抽出とカーブ書き換えを行い、descriptor を直接
見ていないので、順序を後ろへ動かしても手を入れる箇所は無かった (変更は `Configure()`
の1語)。なお VRC Parameter Compressor は `BuildPhase.Optimizing` なので無関係。

### 編集時と ビルド時の乖離

インスペクタの一覧は編集時に作るので、ビルド時にトグルを生成するツールのぶんは
まだ存在せず、出せない。実測では Avatar Menu Creator (Narazaka) が該当し、
MA Object Toggle も同様。`m_IsActive` を生成しうる NDMF ツールは無数にあるため、
**一覧を完全にすることは原理的にできない**。

乖離は両方向に出る。ビルド中に階層を張り替えるパッケージ (AvatarPoseSystem 等) では
編集時とビルド時でパスが別物になり、**一覧に出たのに変換されない**候補も生じる
(実測: `APS_HandleEx/HandleMesh`)。実際に変換したパスはビルドログへ
出力するので突合できる。

Merge Animator が `Relative` のときはカーブパスがコンポーネント基準なので、
`relativePathRoot` (未設定ならコンポーネントの位置) をアバタールート相対へ直して
前置してから照合する。

### その他

機構によらず共通:

- 候補の抽出と絞り込み (入れ子トグルは外側が優先、内側は落とす)
- 対象を常時アクティブ化し、元の `m_IsActive` カーブを機構のバインディングへ書き換える
- `rootBone` / `localBounds` / `updateWhenOffscreen` を正規化して AAO の
  `CategorizationKey` を揃える (統合を可能にする本体)

機構ごとの差分は `HidePlan` を返すバックエンドに閉じている。

## 4つの機構

| 機構 | ボーン | コンストレイント | PB | マテリアル | 消えかた | Mobile |
|---|---|---|---|---|---|---|
| シェイプ(関節) | ±0 | ±0 | ±0 | 統合される | 骨格上へ畳まれる | 可 |
| シェイプ(軸) | ±0 | ±0 | ±0 | 統合される | 骨格上へ畳まれる | 可 |
| UVタイル破棄 | ±0 | ±0 | ±0 | 統合される | 完全に消える | 不可 |
| NaNimation | 増える | ±0 (※) | ±0 | 統合される | 完全に消える | 可 |

※ 初期非表示のトグルでもコンストレイントは増えない (下記「初期非表示の扱い」)。

### シェイプ(関節) — `HideMethod.BlendShape`

全頂点をドミナントボーンの原点へ寄せるブレンドシェイプを生成し、0⇔100 で切り替える。
ランクの計上項目を一切増やさず Mobile でも使える既定の機構。

隠した形状は消滅せず骨格上へ畳まれるので、体で覆われない部位では畳まれた面が見える。

### シェイプ(軸) — `HideMethod.BlendShapeAxis`

寄せ先を関節原点ではなくボーンの軸線分にする。関節へ集めると隣り合う関節を結ぶ帯が
親ボーンの座標系に固定されて動かず、周囲の体表はポーズで逃げるため帯だけが露出する。
軸へ吸着させれば畳まれた面が手足の内部に沿って残り、手足と一緒に動く。

純粋な軸射影は複数ボーンに跨る頂点のポーズ安定性を失う (厳密に一致するのは関節原点
だけ) ため、関節原点からのオフセットを選んだボーンのウェイトで縮めている。

### UVタイル破棄 — `HideMethod.UVTileDiscard`

lilToon の UDIM Discard を使う。メッシュの **UV3** の全頂点へ「4x4 グリッドのどのタイルに
居るか」= タイル中心 `(col+0.5, row+0.5)` を書き、`_UDIMDiscardRow{row}_{col}` を 0⇔1 で
切り替える。`_UDIMDiscardMode = 0` (頂点モード) は頂点シェーダで `positionCS` を NaN に
するので、NaNimation と同じ「クリップ段でプリミティブが破棄される」挙動をボーンを1本も
増やさずに得られる。

セットアップの3プロパティ (`_UDIMDiscardCompile` / `Mode` / `UV`) は shader_feature
キーワードではなく CBUFFER 内の float uniform で、頂点シェーダ側も実行時 if で見ている
(`lil_common_input_base.hlsl:502-505`, `lil_common_vert.hlsl:413-423`)。
**したがって定数カーブで駆動でき、マテリアルを書き換える必要が無い = 複製が要らない。**
複製すると AAO がマテリアルスロットを統合できなくなるので、ここは大きい。

判定に UV3 を使うので UV0 には触らない。UV3 を持たないメッシュには GPU が 0 を供給し、
タイル(0,0) は誰もフラグを立てないので破棄されない。よって UV3 を持たないレンダラーが
同じマテリアルを共有していても巻き込まれない。

適用条件は2つだけ:

- マテリアルが `_UDIMDiscardCompile` を持つ (lilToon 系)。lilToon Lite は
  `ltspass_lite_*` がこのプロパティを宣言していないので自動的に弾かれる。
  Lite は `!defined(LIL_LITE)` で頂点モード自体が無効なので、これは正しい挙動
- メッシュが UV3 を使っていない

制約:

- Mobile のシェーダーホワイトリストに該当機能を持つものが無いので PC 限定
- タイルは 16 個。(0,0) は「UV3 を持たないメッシュ」の居場所なので実際に使えるのは 15 個

UV2/UV1 へのフォールバックは作っていない。UV3 が埋まっていたら `CanApply` が false を
返して別機構へ回る (アバターメッシュで UV3 が埋まっているのは稀)。

#### AAO の統合を阻む条件と回避

`AutoMergeSkinnedMesh.CategorizationKey` には `RendererAnimationLocations` が含まれる。
トグルごとに違うタイルのプロパティを animate すると、レンダラーごとにキーが変わって
別グループへ分かれ、**全く統合されない** (実測: SMR 11 → 11)。

**回避: この機構を使う全レンダラーへ、全トグル分のカーブを付ける。** キーが揃って統合
対象になる。各メッシュは自分のタイルにしか居ないので、他のタイルのフラグが立っても
何も消えない。統合後は1つのレンダラーの全マテリアルへプロパティが及ぶので、統合前に
その状態を先取りしていることになる。

### NaNimation — `HideMethod.NaNimation`

ボーンを複製してスケールを NaN⇔1 で切り替える。`boneCount` が増えるため、ランクを
詰める用途には使えない。**UVタイル破棄が使えない場合 (Mobile ビルド / 非 lilToon) の
退避先。**

複製ボーンが PhysBone 鎖の子に入ると `physBoneTransformCount` が膨らむ
(Shinano 132 → 284) ので、`VRCPhysBone.ignoreTransforms` へ登録して除外している。

## 破綻の2類型

シェイプ方式で畳んだあとに面が残る原因は2つあり、対策が別。

### ポーズ依存型 (タイツ・スラックス・ソックス)

静止時はほぼ消えているのに関節を曲げると飛び出す。→ **シェイプ(軸)** で解決。
コストゼロ。目視確認済み。

### 静止残留型 (スカート・ドレス・コート)

ポーズによらず面が残る。複数のボーンチェーンに跨り、そのボーンが体外にあるため
原理的に一点へ畳めない (Shinano のスカートは静止時点で元の面積の 54% が残る)。
軸射影ではむしろ悪化する。→ **UVタイル破棄** (PC) または **NaNimation** (Mobile)。

### 着座ポーズでの可視面積 (cm²)

| メッシュ | 元 | 関節 静止→着座 | 軸 | 軸+ウェイト縮小 |
|---|---|---|---|---|
| Shinano タイツ | 4671 | 80 → 699 | 343 → 436 | 243 → 355 |
| Velno スラックス | 14240 | 0 → 1536 | 245 → 555 | 127 → 453 |
| Selena セーラー襟 | 3581 | 1301 → 1417 | 953 → 1001 | 1072 → 1103 |
| Shinano スカート | 7373 | 3964 → 3968 | 5262 → 5265 | 4874 → 4877 |
| Hanka コート | 19873 | 4439 → 5024 | 5035 → 5778 | 4757 → 5315 |

## 初期非表示の扱い

初期状態で非表示のトグルは、アニメーターが最初のフレームを適用するまで隠蔽が効かない。
機構ごとの扱い:

- **シェイプ**: ブレンドシェイプのウェイトをメッシュへシリアライズする
- **NaNimation**: 複製ボーンの `localScale = Vector3.zero` をシリアライズする。
  Unity は Transform へ NaN を書けない (バリデーションで弾かれる) が、0 なら書ける。
  0 は「面積ゼロの縮退三角形」でラスタライズ段で破棄されるので、NaN と塗りのコストは
  同じ。これで VRCScaleConstraint が不要になった (MUMUS: 161 → 17)
- **UVタイル破棄**: セットアップ値がシリアライズに要るので、**そのトグルだけ**
  マテリアルを複製する (複製したマテリアルはスロット統合を諦める)。
  `skipInitiallyHiddenMaterialClone` で省けるが、ロード直後に一瞬見える

### WD ON でも成立する

Write Defaults ON は「レイヤーが触っていないプロパティを、アニメーター開始時点の
シリアライズ値へ書き戻す」挙動。書き戻し先が我々の仕込んだ値そのものなので、
WD ON でも初期非表示は保たれる。

## 実測 (Shinano Variant、AAO 込み、Unity 2022.3.22f1)

| 構成 | SMR | MatSlots | Bones | Constraints | PBXform |
|---|---|---|---|---|---|
| 変換なし | 11 | 14 | 272 | 28 | 132 |
| 全シェイプ(関節) | 2 (Good) | 6 (Good) | 272 | 28 | 132 |
| 全シェイプ(軸) | 2 (Good) | 6 (Good) | 272 | 28 | 132 |
| 全UVタイル | 5 | 9 | 272 | 28 | 132 |
| 3着だけ NaNimation | 2 (Good) | 6 (Good) | **442 (VeryPoor)** | 28 | 132 |
| 3着だけ UVタイル | 3 (Medium) | 7 (Good) | 272 | 28 | 132 |
| 全 NaNimation | 2 | 6 | 469 | 28 | 132 |

「3着」は静止残留型の dress / skirt / sweater。Bones=272 は変換なしと同値、つまり
アバター素の骨格。**総合ランクは全カテゴリの最悪値で決まるため、静止残留型の退避先
としては UVタイル破棄が NaNimation より優れる。**

### トグル単位の使い分けの効き幅 (AAO 無し、ボーン数)

| アバター | 既定のみ | 混在 | 全 NaNimation | 削減率 |
|---|---|---|---|---|
| Selena | 244 | 258 | 332 | 84% |
| Hanka | 169 | 205 | 277 | 67% |
| MUMUS_all | 550 | 820 | 1085 | 50% |
| Velno | 148 | 180 | 204 | 43% |
| Shinano | 275 | 445 | 474 | 15% |

効き幅はアバターの構成に強く依存する。使い分けが効くのは「問題が出るのが少数の衣装で
残りが小物」のとき。Shinano では問題の dress/skirt/sweater がボーンの大半を占める衣装
そのものなので 15% しか削れない。

## 前提となる確認済み事実

### ランクの計上項目 (SDK の閾値アセット)

`com.vrchat.base/Runtime/VRCSDK/Dependencies/VRChat/Resources/Validation/Performance/StatsLevels/`

| 項目 | PC Excellent | PC Good | PC Medium | PC Poor | Quest Excellent | Quest Good | Quest Medium | Quest Poor |
|---|---|---|---|---|---|---|---|---|
| skinnedMeshCount | 1 | 2 | 8 | 16 | 1 | 1 | 2 | 2 |
| meshCount | 4 | 8 | 16 | 24 | 1 | 1 | 2 | 2 |
| materialCount | 4 | 8 | 16 | 32 | 1 | 1 | 2 | 4 |
| boneCount | 75 | 150 | 256 | 400 | 75 | 90 | 150 | 150 |
| constraintsCount | 100 | 250 | 300 | 350 | 30 | 60 | 120 | 150 |
| polyCount | 32000 | 70000 | 70000 | 70000 | 7500 | 10000 | 15000 | 20000 |
| textureMegabytes | 40 | 75 | 110 | 150 | 10 | 18 | 25 | 40 |
| physBone componentCount | 4 | 8 | 16 | 32 | 4 | 8 | 16 | 32 |
| physBone transformCount | 16 | 64 | 128 | 256 | 0 | 16 | 32 | 64 |
| physBone colliderCount | 4 | 8 | 16 | 32 | 4 | 8 | 16 | 32 |
| physBone collisionCheckCount | 32 | 128 | 256 | 512 | 32 | 128 | 256 | 512 |

**ブレンドシェイプ数・頂点数・メッシュメモリは計上対象に存在しない。**
polyCount は三角形数であり、頂点を潰しても複製しても変わらない。

### Mobile のシェーダー制限

`AvatarValidation.ShaderWhiteList` の10種のみ許可される (`VRChat/Mobile/*`)。
lilToon も Poiyomi も含まれない。10種のソースを全て確認したが **`_Cutoff` を公開して
いるものは一つも無い**。したがって Mobile ではシェーダー側で隠す機構が全て使えない。

### 縮退三角形と NaN の実コスト差は小さい

頂点を1点へ潰した三角形は面積ゼロになりラスタライズ段で破棄される。NaN の三角形は
クリップ段で破棄される。**どちらも塗りのコストは発生しない。** 残るのは頂点処理の
コストで、そこは同等 (ブレンドシェイプ評価 vs ボーン行列)。

## 没案とその理由

| 案 | 不成立の理由 |
|---|---|
| ボーン部分木の scale 0 | 成立条件は「その部分木が他メッシュに一切使われていない」こと。実測 (5体62レンダラー) で頂点が部分木内で完結する割合は Shinano のスカート・タイツで 0%、Selena のスカートで 27%。高い値が出たのは元々問題にならない小物だけ |
| 単一 NaN ボーンを全メッシュに追加 | Unity は Transform へ NaN スケールを書けない。加えて `Mesh.SetBoneWeights` が 4 ボーン/頂点に切り詰めるので5本目のウェイトを持てない (実測 nanVerts=0) |
| マテリアルスワップ / アルファ・カットオフ | Mobile 不可 (上記シェーダー制限)。PC では UVタイル破棄で足りる |
| 静的削除 | トグルにならない |
| AAO `RemoveMeshByUVTile` への相乗り | 静的削除なのでトグルにならない |
| インデックスバッファ破棄 | アニメーションのバインディング対象外 |
| 頂点カラー破棄 | lilToon / Poiyomi に経路が無い。カスタムシェーダは方針外 |
| 遠方への吹き飛ばしシェイプ | 回転差の大きいポーズで巨大三角形が視界を掠める。ワールドの far clip 依存で品質保証ができない |
| 統合グループをカーブで揃える | 実装して実測したが効果ゼロ (SMR 3 / MatSlots 7 のまま)。`GetAnimationLocationsForRendererAnimation` は `blendShape.` で始まるプロパティを明示的に除外する (`AutoMergeSkinnedMesh.cs:503`) ため、シェイプ方式のレンダラーの `RendererAnimationLocations` はもともと空で、前提が成り立っていなかった。真因は未特定 |

**第5の隠蔽 primitive は存在しない。** アニメーション可能で描画に影響する要素は
{activeness (統合を壊す)・Transform (ボーン系)・blendshape・マテリアル float・
マテリアルスワップ} で全て。スクリプト不可なのでインデックスバッファ系は動かせない。
blendshape で任意ポーズの縮退を満たすには三角形の3頂点が同一ウェイトである必要が
あり (スキニングの線形性)、複数チェーン跨ぎでは原理的に不可能。

## 指標選びの教訓

この件で指標を3回間違えている。辺の長さ (共線に潰れた三角形は見えない) →
面の太さ (静止時しか見ない・太さ 1cm の帯は普通に見える) → ポーズを付けたときの
可視面積。間接的な指標で判断すると、もっともらしい誤結論に到達する。

可視面積も「体表の外に出るか」までは測れていない。関節集約は面積が小さくても帯が
露出し、軸射影は面積が大きくても手足の内部に留まる。**最終判断は目視。**

## 開発メモ

- AAO の `MeshInfo2` は `-nographics` に非対応。AAO を通すベンチは `-batchmode` のみで
  走らせる
- 計測足場は `DevProject/Assets/_MTBench/Editor/MTBench.cs` (`BenchMixedAAO` が上表を出す)

## 機構の自動割り当て

`MethodAdvisor` (インスペクタの「自動で割り当てる」)。判定は**畳んだあとに実際どれだけ
面が見えるか**の実測で行う。太さや辺長のような間接指標は使わない (上記「指標選びの教訓」)。

各トグルについて、関節寄せと軸吸着の畳んだ頂点位置を作り、**静止時**と**着座ポーズ**の
両方でスキニングした三角形面積の総和を測る (ポーズは一時的に付けて必ず戻す)。

| 条件 | 割り当て |
|---|---|
| 静止残存 > 10% **かつ** > 500cm² | UVタイル破棄 |
| 上記以外で、着座時の関節寄せ残存 > 5% かつ 軸 < 関節 × 0.9 | シェイプ(軸) |
| それ以外 | シェイプ(関節) |

UV タイル破棄に回ったものは残存の大きい順に 15 枠を配り、あぶれたものと適用条件を
満たさないものは NaNimation へ落とす。

### 閾値の根拠 (5体62トグルの実測)

- **静止残存比 10%**: 畳める群 (タイツ 1.7%、スラックス 0%) と畳めない群
  (コート 22%、セーラー襟 30%、スカート 54%) の間が広く空いている。
- **静止残存面積 500cm²**: 比率だけだと小さいメッシュを拾いすぎる。
  回すべきもの 1300〜9688cm² に対し、回す必要のない小物は 13〜137cm² で、間が広い。
  **UV タイル破棄はシェイプ方式と別の統合グループへ分かれるため、1件増やすと SMR と
  マテリアルスロットを損することがある** (実測: Shinano で 3件→5件にすると
  SMR 3→4、MatSlots 7→9 でランクが Good から Medium へ落ちた)。「タイルが余っているから
  多めに回す」は成立しない。
- **軸の下限 5%**: 相対比だけだと誤差で既定から動く (MUMUS の獣耳が 2.5% 対 2.2% で
  軸へ倒れた)。ポーズ依存型の実測は Shinano タイツ 15%、Velno スラックス 11% なので、
  その下に置けば取りこぼさない。

### 実測 (Shinano、AAO 込み)

| 構成 | SMR | MatSlots | Bones | 備考 |
|---|---|---|---|---|
| 全シェイプ(関節) | 2 | 6 | 272 | タイツが着座で貫通する (目視確認済み) |
| 手で3着 UVタイル | 3 | 7 | 272 | dress/skirt/sweater |
| **自動割り当て** | **3** | **7** | **272** | 上記3着 + tights を軸へ |

自動割り当ては手で選んだ構成に並び、さらに手動では気づいて指定する必要があった
タイツの軸方式まで拾う。
