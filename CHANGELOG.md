# Changelog

## [0.8.2-alpha]

**検出・変換のロジックは無変更。インスペクタの表示だけを変えた版。**

### 追加

- **トグルの一覧へ「そのトグルで隠れる頂点数」を出すようにした。** 合計も出す。
  隠しても統合したメッシュは常に表示されるので、変換したトグルは隠している間も
  頂点ぶんのコストを払い続ける。実測 (Shinano_TEST・10 体・8 反復・対の差、
  塗り面積 × 追加描画回数の 2x2) では、変換対象の頂点のうち普段隠している割合が
  9 割を超えたあたりで損得が釣り合った。CPU メインスレッドで見た分岐点は 4 構成すべて
  89.1〜89.8% で構図に鈍感だった。つまり**ほとんどの人は既定のまま全部変換して得**である。
  根拠は `Docs/hidden-cost-decision.md` と `Docs/perf-research-backlog.md` の目標 E

  **この頂点数は状況の把握用であって、除外を勧めるためのものではない。** 目標 F の実測で、
  頂点数の大きいトグルを外しても**隠している間の CPU 側の損は減らない**ことが分かっている
  (減るのは GPU 側だけで、表示中の利益は確実に失われる)。詳細は同 §7

### ドキュメント

- README へ「隠している間のコスト」の節を追加した。統合したメッシュは常に表示された
  ままなので、変換したトグルは隠している間も頂点ぶんのコストを払い続ける、という
  トレードオフと、その判断の目安を書いた。実測条件は `<details>` へ畳んである

## [0.8.1-alpha]

### 修正

- **隠蔽シェイプのデルタを `+Infinity` から有限の遠方値 (`1e6`) へ変えた。**
  ∞ デルタを持つメッシュは、VRChat クライアントの自分視点 (一人称・カメラ) で
  レンダラーごと描画されなくなる。鏡では正常に見え、Unity エディタでも Av3Emulator でも
  再現しないため気付きにくい。隠蔽シェイプのウェイトが 0 (表示状態) でも起きるので、
  **メッシュに ∞ デルタが存在すること自体**が引き金である。実機で「素のアバター: 正常 /
  AAO 単独: 正常 / 本パッケージ単独: 変換対象だけ消える」まで切り分け、有限値にすると
  表示されることを確認した。消えかた・ランクの計上・AAO の統合結果は従来と変わらない
  (遠クリップ面の外へ飛ぶので描画されず、スキニング後の散らばりも最大 8m)。

## [0.8.0-alpha]

### 変更

- **`rootBone` の正規化先を「変換対象以外の SMR が最も多く共有している値」にした**
  (従来は Humanoid の Hips へ固定)。MA Mesh Settings 等でアバター内に別の合意が
  成立している場合、Hips への固定はその合意から外れて AAO の統合グループを割り、
  正味の効果がゼロになることがあった (実測: APS 入りアバターで SMR 5→5)。
  変換対象以外の SMR が 1 つも無い場合は従来どおり Hips へ倒す。
  検出・変換・PhysBone 停止のロジックは変更していない。

## [0.7.0-alpha]

### 追加

- **複数トグルで共有する PhysBone も停止できるようにした。**
  `disableSharedPhysBonesWhenHidden` (既定オン) を追加。共有 PB の全消費レンダラーを
  所有するトグルが全部非表示になったときだけ、`m_Enabled` を 0 にする。
- 共有 PB の所有トグル集合ごとに FX へ `MT_PBStop <n>` レイヤーを生成し、
  `MT_Hidden/<トグルのパス>` ローカル float パラメータの AND 条件で停止する。
  同期パラメータではないため Expression Parameters は消費しない。
- 所有トグルの元クリップが FX 以外にも属する共有グループは、AAP がコントローラを
  またげないため保守側へ倒してスキップし、ビルドログへ警告する。

### 修正

- 生成する全トグルカーブをステップ補間にし、元の `m_IsActive` と同じ離散動作にした。
- 配下コンポーネントの編集時無効、既存 `m_Enabled` アニメーション、同型複数配置を
  保守側でスキップし、初期非表示時は生成対象をシリアライズ時点から無効にした。
- 初期非表示の単独所有 PhysBone は、シリアライズ時点から `enabled=false` にした。
- PhysBone の停止判定を、実際に隠蔽または無効化できる Renderer だけに基づくようにした。
- FX と FX 外で共有されるクリップ、既存の `MT_Hidden/<path>` パラメータと衝突する
  共有 PhysBone グループをスキップするようにした。
- 全 owner が初期非表示の共有 PhysBone グループは、停止ステートから開始するようにした。
- インスペクタで除外・強制対象リストが `null` の場合にも描画できるようにした。

## [0.6.0-alpha]

### 追加

- **非表示中は専用 PhysBone も止める** (`disablePhysBonesWhenHidden`、既定オン)。
  衣装トグルで隠したメッシュのためだけに存在するアーマチュア側の PhysBone を、
  隠蔽と同じカーブで `m_Enabled` = 0 にする。判定は保守側
  (チェーンの消費レンダラーが全部そのトグルで隠れる場合のみ。素体と共有する
  胸・尻尾・共用スカート等は止めない)。何を止めたかはビルドログに出る。
  再表示時の揺れはレスト位置から始まる (Unity の仕様)。
  実測: Shinano 10/61 本・MUMUS_all 68/89 本を停止、AAO の統合結果は不変。

## [0.5.0-alpha]

隠しかたを **infinimation の 1 本**にした。**破壊的変更**（`HideMethod` と
トグルごとの機構上書きが無くなる）。

### 追加

- **infinimation** — 全頂点のデルタを `+Infinity` にしたブレンドシェイプを生成し、
  0⇔100 で切り替える。頂点が非有限座標へ飛んでプリミティブがクリップ段で破棄されるため
  完全に消えるが、ボーン・コンストレイント・マテリアル・ポリゴンはひとつも増えず、
  シェーダにもプラットフォームにも依存しない。初期非表示はシェイプのウェイトとして
  そのままシリアライズできる。

  NaN デルタは Unity が格納時に 0 へ潰すので `+Infinity` で作る（実測）。

  AAO 込みの実測: Shinano SMR 11→2・MatSlots 14→6・ボーン 272 のまま /
  MUMUS_all SMR 21→4・MatSlots 36→18・ボーン 453 のまま。ポリゴン数は不変。
  Play（Av3Emulator）と PC 実機でも、トグル往復・初期非表示・カリング・ランク表示に
  問題が無いことを確認した。**Quest（モバイル GPU）の実機でも、隠したメッシュが
  完全に消え、表示中のメッシュに破綻が出ないことを 2026-08-25 に確認した**
  （遠近のカリングと Performance ランク表示は未報告）。

### 削除（破壊的変更）

- **既存 4 機構を退役させた** — シェイプ(関節) / シェイプ(軸) / UVタイル破棄 /
  NaNimation。infinimation がすべての面で上回ったため。それぞれ、畳み残しが見える /
  同左 / lilToon 専用・PC 限定・15 枠上限 / boneCount が増える、という弱点があった。
- **`HideMethod` と `MethodOverride` を削除した。** トグルごとの機構選択が無くなるため、
  インスペクタの機構ポップアップと「自動で割り当てる」も消えた。
- **`MethodAdvisor` を削除した**（機構が 1 つになり、割り当てを決める必要が無くなった）。
- **`skipInitiallyHiddenMaterialClone` を削除した**（UV タイル破棄専用の設定だった）。

`hideMethod` / `methodOverrides` / `skipInitiallyHiddenMaterialClone` を設定していた
Prefab は、その値が失われる（変換自体は infinimation で行われる）。

## [0.4.0-alpha]

一覧が読めない問題への対応。**検出・変換のロジックは変えていない。**

### 変更

- **一覧を持ち主ごとの折りたたみにした。** 平らな一覧では GameObject 名だけが並び、
  実アバターで `HandleMesh` が 6 行続いて判別できなかった。

  ```
  ▼ AvatarPoseSystem (6件)                [✓]
       APS_Handle/HandleMesh
       APS_HandleEx/HandleMesh
       …
  ▼ Milfy_DoubleHand_Penlight (2件)       [–]
       右手/ペンライト空中固定/ペンライト座標
       左手/ペンライト空中固定/ペンライト座標
  ```

  見出しは入れ子プレハブの根を優先し、無ければ導入単位（アバタールートから 2 段目）。
  親を 1 段だけ上がる方式では粒度が細かすぎて、1 件ずつのグループに割れたうえ
  「ペンライト空中固定」が重複した。
- グループ単位の一括選択（三状態）を追加。
- **行名を判別可能にした。**
  - メニューから駆動されているトグルは、メニュー上の表示名を出す。
  - 名前が重複する行・ラベルが無い行は、**他の行と区別が付くところまで**
    パス末尾を伸ばして添える。段数固定では足りない（実測: 左右のペンライトは
    3 段上でしか分かれない）。
  - 複数のトグルが同じクリップを共有している場合はラベルを出さない。
    どのトグルのラベルか決められないため（実測: 右手のペンライトに
    `ペンライト出現_L` が付いた）。誤ったラベルを出すよりパス表示を選ぶ。

### 既知の制限

- **メニュー名が出るのはトグルごとにクリップが分かれている場合だけ。**
  1 クリップを複数トグルで使い回している構成（APS のハンドル、左右のペンライト等）
  ではパス表示になる。実測した 2 アバターはいずれもこの構成で、メニュー名は 0 件だった。
- **インスペクタの一覧は変換結果の予告ではない。乖離は両方向に出る。**
  この一覧は参考表示であり、正はビルドログ。
  - **ビルド時にトグルを生成するツールのぶんは一覧に出ない。** 編集時にクリップが
    存在しないため。衣装アバター（02_SnowflakeRomance）の実測で一覧 8 件に対し
    ビルド 18 件。差分 12 件の出どころは **Avatar Menu Creator (Narazaka)** だった
    （到達できる 1207 本のクリップを全走査してヒット 0、コンポーネント棚卸しで特定）。
    MA Object Toggle も同様に出ない。**m_IsActive を生成しうる NDMF ツールは
    無数にあるため、一覧を完全にすることは原理的にできない。**
  - **逆に、一覧に出ても変換されないものがある。** ビルド中に階層を張り替える
    パッケージ（AvatarPoseSystem など）では編集時とビルド時でパスが別物になる
    （実測: `APS_HandleEx/HandleMesh`。2 アバターで再現）。
  - 実際に何が変換されたかはビルドログ（`[MergeableToggle] thickness ...`）で確認する。

## [0.3.0-alpha]

### 破壊的変更

- **パッケージ ID を `com.kie.mergeable-toggle` から `com.kie.kie-mergeable-toggle` へ
  変更した。** 作者の他パッケージと命名を揃えるため。VPM 上は別パッケージとして
  扱われるので、旧 ID で入れている場合は一度削除してから入れ直すこと。
- GitHub リポジトリも `Kie610/kie-mergeable-toggle` へ改名した
  (旧 URL からはリダイレクトされる)。
- displayName を `kieMergeableToggle`、コンポーネントメニューを
  `Kie/kieMergeableToggle` へ変更した。既に付けてあるコンポーネントは影響を受けない。

旧 ID の 0.1.0-alpha / 0.2.0-alpha は動作が不十分だったため Release ごと破棄した。

## [0.2.0-alpha]

### 修正

- **Modular Avatar の MA Merge Animator 経由で合流するトグルを検出できなかった。**
  実行順を MA より前から**後**へ変更 (`AfterPlugin`)。0.1.0-alpha は、そういう構成の
  アバターでは一覧に何も出ず、ビルドしても何も変換していなかった。
  実測: FX コントローラを Merge Animator へ移すと検出 9 件 → 0 件だったものが 9 件に戻る。

### 追加

- **MA がリアクティブに生成するトグル (MA Object Toggle など) もビルド時の変換対象に
  なった。** 合流後は手書きトグルと区別が付かないため。除外は従来どおり可能。
- インスペクタが MA Merge Animator も辿るようになった (`Relative` パスモードと
  `relativePathRoot` に対応)。Modular Avatar が無い環境でも動く (ソフト依存)。
- 変換したトグルのパスと機構をビルドログへ出力。インスペクタの一覧と突合できる。

## [0.1.0-alpha]

初回リリース。アニメーターの `m_IsActive` トグルを検出し、ビルド時に別の隠しかたへ
変換して AAO の自動メッシュ統合を可能にする。

### 隠しかた

- **シェイプ(関節)** — 全頂点をドミナントボーンの原点へ寄せるブレンドシェイプ。既定。
- **シェイプ(軸)** — 寄せ先をボーンの軸線分にすることで、関節を跨ぐ衣装が
  ポーズで飛び出す破綻を抑える。
- **UVタイル破棄** — lilToon の UDIM Discard。UV3 へタイル中心を書き、破棄フラグを
  animate する。ボーンもコンストレイントも増やさずに完全に消える。PC 限定・15 枠まで。
- **NaNimation** — ボーンを複製してスケールを NaN にする。Mobile ビルドと
  非 lilToon マテリアル用の退避先。

### 機能

- アバタールート用コンポーネント `Mergeable Toggle`。候補一覧から変換対象を選択でき、
  トグルごとに機構を上書きできる。
- **自動割り当て** — 畳んだあとの可視面積を静止時と着座ポーズで実測して機構を決める。
- 非表示中に、トグル配下のコンポーネントの `m_Enabled` も落とす (既定で有効)。
- 選んだ機構が適用できない候補を「適用不可」として表示する。

### 既知の制限

- Mobile (Quest) では UVタイル破棄が使えないため NaNimation へ落ちる。ビルドターゲット
  による自動切り替えは未実装。
- UVタイル破棄はメッシュが UV3 を使っていないことが条件。UV2/UV1 へのフォールバックは
  未実装。
- 実機での検証は限定的。
